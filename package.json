{
  "name": "diary-app",
  "version": "1.0.0",
  "description": "Diary App",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ioredis": "^5.3.2"
  }
}
